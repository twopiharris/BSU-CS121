```
create true and false constants
create int for correct
create int for guess
create int for turn init to 0
create boolean keepGoing
create char[] for name

ask user name, put in name
greet user with name

generate a random nunber 1-100 put in correct

while keepGoing is true:
  increment number of turns
  ask user for guess, put in guess
  if guess < correct:
    print "too low"
  if guess > correct:
    print "too high"
  if guess is correct:
    print "Right!"
    set keepGoing to false


```
